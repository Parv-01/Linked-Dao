{
  "name": "linked-dao-hypergraph",
  "version": "1.0.0",
  "description": "Hypergraph indexing for LinkedDao OnchainTrustNetwork",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy",
    "create-local": "graph create --node http://localhost:8020/ linkedDao/trust-network",
    "remove-local": "graph remove --node http://localhost:8020/ linkedDao/trust-network",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 linkedDao/trust-network"
  },
  "keywords": ["hypergraph", "blockchain", "indexing", "linkedDao"],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@graphprotocol/graph-cli": "^0.97.1",
    "@graphprotocol/graph-ts": "^0.38.1"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}