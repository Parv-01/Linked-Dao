{
  "name": "onchain-trust-network-subgraph",
  "version": "1.0.0",
  "description": "Traditional subgraph for OnchainTrustNetwork contract events",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ onchain-trust-network",
    "create-local": "graph create --node http://localhost:8020/ onchain-trust-network",
    "remove-local": "graph remove --node http://localhost:8020/ onchain-trust-network",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 onchain-trust-network",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "^0.69.0",
    "@graphprotocol/graph-ts": "^0.32.0"
  },
  "devDependencies": {
    "matchstick-as": "0.5.0"
  }
}
